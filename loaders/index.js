const userLoader = require('./user');
const channelLoader = require('./channel');
const repliesLoader = require('./replies');
const favoritesLoader = require('./favorites');

module.exports = {
  userLoader,
  channelLoader,
  repliesLoader,
  favoritesLoader,
};
